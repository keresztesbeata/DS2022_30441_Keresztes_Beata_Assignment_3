let config = {
    host    : 'localhost',
    user    : 'root',
    password: 'root',
    database: 'energy-db',
    waitForConnections: true,
    connectionLimit: 100,
    queueLimit: 0
};

module.exports = config;